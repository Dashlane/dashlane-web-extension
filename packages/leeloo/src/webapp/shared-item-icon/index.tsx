import * as React from 'react';
import sharedCredentialImg from './img/shared-icon.svg';
import styles from './styles.css';
const SharedItemIcon = () => (<img className={styles.sharedIcon} src={sharedCredentialImg}/>);
export default SharedItemIcon;
