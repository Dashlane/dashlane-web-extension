import { jsx } from '@dashlane/design-system';
export const SearchContainer = (props: any) => (<div sx={{
        display: 'flex',
        alignItems: 'center',
        position: 'relative',
        padding: '12px 16px',
    }} {...props}/>);
