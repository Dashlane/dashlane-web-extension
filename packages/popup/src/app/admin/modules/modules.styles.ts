import { ThemeUIStyleObject } from '@dashlane/design-system';
export const MODULES_SX_STYLES: Record<string, Partial<ThemeUIStyleObject>> = {
    MODULE: {
        border: '1px solid',
        borderColor: 'ds.border.neutral.standard.idle',
        borderRadius: '4px',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        flexDirection: 'column',
        flexGrow: 1,
        padding: '12px',
    },
};
