import * as React from "react";
import { WebcardWrapper } from "./components/webcards/WebcardWrapper";
export const AppWrapper = (props: PlaygroundProps) => {
  return <WebcardWrapper {...props} />;
};
